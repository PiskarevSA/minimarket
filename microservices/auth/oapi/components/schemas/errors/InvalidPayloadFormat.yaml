type: object
properties:
  code:
    type: string
    enum: ["U1402"]
  message:
    type: string
    enum: ["invalid payload format"]
required:
  - code
  - message
x-go-name: "InvalidPayloadFormatResponse"
